{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "resources": [
    {%- for item in resource_data %}
    {
      "type": "Microsoft.Storage/storageAccounts",
      "apiVersion": "2023-01-01",
      "name": "{{ item }}",
      "location": "{{ resource_data[item].properties.location }}",
      "sku": {
        "name": "{{ resource_data[item].properties.sku | default('Standard_LRS') }}"
      },
      "kind": "{{ resource_data[item].properties.kind | default('StorageV2') }}",
      "tags": {{ resource_data[item].properties.tags | default({}) | to_json }}
    }{% if not loop.last %},{% endif %}

    {%- endfor %}
  ]
}
